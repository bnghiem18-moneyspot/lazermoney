/**
 * Configuration Switcher
 * ======================
 * Import different brand configs and switch between them here.
 * To change brands, simply change the import and export.
 * 
 * Example:
 *   import config from './anotherbrand.js';
 *   export default config;
 */

import config from './lazermoney.js';

export default config;
